# PBI-025: Avionics Globals — Typography, Grid Layout, Prose

## Directive
Update `src/styles/globals.css` to implement Avionics typography rules (uppercase headings with expanded width, monospace h3) and add the 6-column CSS Grid layout system with named zones (full, breakout, content). Add `.prose`, `.content-prose`, `.border-box`, `.spec-grid`, and `.spec-list` utility classes. Keep existing reset, skip-link, focus, reduced-motion, and container helpers.

## Context
- Spec: `specs/avionics-design-system/spec.md`
- Depends on: PBI-024 (tokens must exist first)
- Files/modules touched: `src/styles/globals.css`
- Plane: `kcb/KCBLABS-25`

## Verification
- h1, h2, h3 have `text-transform: uppercase`, `letter-spacing: 0.02em`, `font-variation-settings: "wdth" 110`
- h3 uses `font-family: var(--font-mono)`
- `.grid-layout` class defines 6-column grid with named lines
- `.spec-grid`, `.spec-list`, `.border-box`, `.prose` classes exist
- `cmd /c "npm.cmd run check"` passes
- `cmd /c "npm.cmd run build"` succeeds

## Refinement rule
- If reality diverges from the spec: stop and flag for human review
