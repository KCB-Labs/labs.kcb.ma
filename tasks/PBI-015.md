# PBI-015: People, Ecosystem, Roadmap Pages

## Directive
Build the human, partner, and directional surfaces. Create `src/pages/about/people/index.astro` + `src/pages/about/people/[slug].astro` (prerendered, `getPeople()` via helpers, `PersonCard` index ordered `order` then `featured`, detail shows `shortBio`/`bio`, avatar via Astro image with `alt`, `website/linkedin/github/x`, `relatedLabs[]`/`relatedProjects[]` as linked cards), `src/pages/about/ecosystem/index.astro` + `src/pages/about/ecosystem/[slug].astro` (prerendered, grouped by organization `type` Partner/Client/Sponsor/Startup/University/Research Organization/Technology Partner/Community/Open Source Community, `OrganizationCard` grouping, detail shows `logo`/`description`/`relationship`/`website`/`relatedLabs`/`relatedProjects`), and `src/pages/about/roadmap/index.astro` (prerendered, timeline of `roadmap` entries sorted by `period` ascending via `Timeline` component, showing `period`/`theme`/`objective`/`areas`/`status`). All pages remain Astro-only, share layouts/cards/tokens, and handle empty relations gracefully.

## Context
- Spec: `specs/institutional-pages/spec.md`
- Depends on: PBI-008, PBI-014
- Files/modules touched: `src/pages/about/people/index.astro`, `src/pages/about/people/[slug].astro`, `src/pages/about/ecosystem/index.astro`, `src/pages/about/ecosystem/[slug].astro`, `src/pages/about/roadmap/index.astro`, `src/components/cards/PersonCard.astro`, `src/components/cards/OrganizationCard.astro`, `src/components/content/Timeline.astro` (static/interactive base)
- Plane: `kcb/KCBLABS-15`

## Verification
- Spec §Contracts: `npm run build` prerenders `/about/people`, `/about/people/[slug]` for each published person, `/about/ecosystem`, `/about/ecosystem/[slug]`, `/about/roadmap`; unknown slugs → branded 404
- People index unit/integration check: published people count matches helper, order respects `order` then `featured`; person detail for seeded person shows its related labs/projects as linked cards; person with no relations renders empty state without throw (second fixture)
- Ecosystem index groups by `type` correctly — test asserts grouping helper or page data loader matches expected groups; detail shows related labs/projects
- Roadmap page entries sorted ascending `period` and timeline shows theme/objective/status per entry (matches `getRoadmaps()` helper ordering)
- No direct collection queries; `cmd /c "npm.cmd run check"` + `build` pass; single `h1` per page, images `alt`, Astro image pipeline used

## Refinement rule
- If group-by for ecosystem or ordering for roadmap diverges from seeded content expectations, align the page to the helper in `src/lib/content` (source of truth) and update fixtures rather than sorting in-page ad hoc.
