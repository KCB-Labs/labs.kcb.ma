# PBI-032: Entity Rating — Card Component Integration

## Directive
Add `RatingDots` to all 5 core card components (LabCard, ResearchCard, ExperimentCard, ProjectCard, OpenSourceCard). Rating appears in the same row as the `<h3>` title, using flex layout with baseline alignment. Pass rating props from parent pages.

## Context
- Spec: `specs/entity-rating/spec.md`
- Depends on: PBI-030 (RatingDots component must exist)
- Files/modules touched:
  - `src/components/cards/LabCard.astro`
  - `src/components/cards/ResearchCard.astro`
  - `src/components/cards/ExperimentCard.astro`
  - `src/components/cards/ProjectCard.astro`
  - `src/components/cards/OpenSourceCard.astro`
- Plane: — (not synced)

## Verification
- Each card renders `RatingDots` next to the title in a flex row
- Rating props are accepted and passed through
- Cards without rating props still render (backward-compatible)
- Responsive: rating wraps on narrow screens without breaking layout
- `cmd /c "npm.cmd run check"` passes with 0 errors
- `cmd /c "npm.cmd run build"` succeeds

## Refinement rule
- If a card's existing layout conflicts with the rating row, adjust the card layout to accommodate — do not remove the rating
