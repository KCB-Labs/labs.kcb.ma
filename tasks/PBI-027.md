# PBI-027: Avionics Components — Cards, Badges, Footer, Breadcrumbs

## Directive
Update all component styles to use Avionics tokens and design patterns. Cards: border-box style with warm palette, Safety Orange hover border. StatusBadge: monospace, uppercase, 1px border, no border-radius (industrial square). TypeBadge, StageBadge: same pattern. SiteFooter: warm surface background, monospace section labels. Breadcrumbs: monospace, uppercase, muted color. PageHeader: remove label duplication (already done), ensure h1 is uppercase. Section headings: uppercase, expanded width.

## Context
- Spec: `specs/avionics-design-system/spec.md`
- Depends on: PBI-024, PBI-025 (tokens and globals must exist)
- Files/modules touched: `src/components/cards/*.astro`, `src/components/ui/*.astro`, `src/components/astro/SiteFooter.astro`, `src/components/astro/Breadcrumbs.astro`, `src/components/astro/PageHeader.astro`
- Plane: `kcb/KCBLABS-27`

## Verification
- Cards use `var(--c-border)` borders, `var(--c-brand)` hover
- StatusBadge uses `var(--font-mono)`, uppercase, 1px border
- SiteFooter uses `var(--c-bg-surface)` background
- Breadcrumbs use monospace font
- All color values reference tokens, no literals
- `cmd /c "npm.cmd run check"` passes
- `cmd /c "npm.cmd run build"` succeeds

## Refinement rule
- If reality diverges from the spec: stop and flag for human review
