# PBI-011: Projects Index + Project Detail (Origin, Architecture, Outcomes)

## Directive
Implement the Project discovery surface (spec §19). Create `src/pages/projects/index.astro` (prerendered, lists `getProjects()` via `ProjectCard` with filters stub for future PBI-018) and `src/pages/projects/[slug].astro` (prerendered via `getStaticPaths` from `getProjects()`, branded 404 on unknown slug). Project detail uses `ProjectLayout.astro` and renders: header (Project name/description/type stage origin with "Born in KCB Labs" treatment when `originLab` present), sections Overview/Problem/Context/What we discovered/Solution/Architecture/Current State/Roadmap/Outcomes, Research behind it (`getResearchByProject` or via relationships), Experiments, Related Journal, People, Organizations, Open Source, Links. Reuse `StatusBadge`/`TypeBadge`/`StageBadge` for type/stage and `CodeBlock`/`ArchitectureDiagram` stubs. No React islands; filtering deferred to PBI-018.

## Context
- Spec: `specs/core-pages/spec.md`
- Depends on: PBI-008, PBI-009
- Files/modules touched: `src/pages/projects/index.astro`, `src/pages/projects/[slug].astro`, `src/layouts/ProjectLayout.astro`, `src/components/content/ArchitectureDiagram.astro`, `src/components/content/CodeBlock.astro` (static), `src/components/cards/ProjectCard.astro` (enhanced), `src/components/cards/OpenSourceCard.astro` (stub)
- Plane: `kcb/KCBLABS-11`

## Verification
- Spec §Contracts: `npm run build` prerenders `/projects` and `/projects/operant` (seed); `/projects/unknown` → branded 404
- Project detail for `operant` shows `Type: Internal`, `Stage`, `Origin: AI Employees Lab` and the "Born in KCB Labs" block; related research/experiments/journal/people/orgs/opensource lists match helper results (integration test on `/projects/operant` asserts sections + card counts)
- Project index is server-rendered full list (JS disabled shows full list) — baseline for future filters
- No hard-coded projects: deleting all project fixtures reduces project count to zero without page crash (empty state handled)
- SEO/canonical/OG/Twitter emitted via `BaseLayout`; images use Astro pipeline with `alt`
- No direct collection queries; `cmd /c "npm.cmd run check"` + `build` pass

## Refinement rule
- If `Stage` or `Type` enum values diverge from installed schema, align the component to the schema enum (source of truth is `keystatic.config.ts`) and update `specs/content-model` enum list in the PR, then flag for review.
