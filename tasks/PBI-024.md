# PBI-024: Avionics Tokens — Color Palette & Typography Tokens

## Directive
Replace all color and typography tokens in `src/styles/tokens.css` with the Avionics International Safety palette. Map old tokens to new Avionics values. Add Google Fonts import for Archivo and B612 Mono. Keep all existing token names intact (no component changes needed). Preserve dark theme support with Avionics dark tokens.

## Context
- Spec: `specs/avionics-design-system/spec.md`
- Depends on: none
- Files/modules touched: `src/styles/tokens.css`
- Plane: `kcb/KCBLABS-24`

## Verification
- `src/styles/tokens.css` contains Avionics palette: --color-bg: #f4f4f0, --color-surface: #ebebe6, --color-text: #111111, --color-accent: #f04e30
- --font-display and --font-body set to "Archivo", --font-mono set to "B612 Mono"
- Dark theme block uses Avionics dark tokens
- `cmd /c "npm.cmd run check"` passes (0 errors)
- `cmd /c "npm.cmd run build"` succeeds

## Refinement rule
- If reality diverges from the spec: stop and flag for human review
