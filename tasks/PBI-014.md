# PBI-014: About + Vision Pages (Singletons, Editorial Layout)

## Directive
Implement the institutional entry points for mission and direction. Create `src/pages/about/index.astro` (prerendered, hub linking to vision/people/ecosystem/roadmap with sections built from `Prose`/`Section`/`Metric`) and `src/pages/about/vision.astro` (or `src/pages/about/vision/index.astro` depending on routing decision, but canonical URL must be `/about/vision`) sourcing singleton `pages/vision` (or `pages/about` where applicable) via `src/lib/content` helpers. Both pages use `BaseLayout` SEO props, tokens, and editorial whitespace; vision supports headings/prose/timeline/metric_diagram rendering. Document the IA choice for `/about/vision` vs `/about/vision/` in `ARCHITECTURE.md` stub. No people/ecosystem/roadmap lists here (next PBI).

## Context
- Spec: `specs/institutional-pages/spec.md`
- Depends on: PBI-003, PBI-008
- Files/modules touched: `src/pages/about/index.astro`, `src/pages/about/vision/index.astro` (or `.astro`), `src/lib/content/roadmap.ts` (read-only use if vision references roadmap), `src/components/content/Metric.astro` (reuse), `ARCHITECTURE.md` (IA note)
- Plane: `kcb/KCBLABS-14`

## Verification
- Spec §Contracts: `npm run build` prerenders `/about` and `/about/vision` to `dist/` HTML; both return 200 and emit `title`/`description`/`canonical`/OG/Twitter via `BaseLayout` (view-source check); vision content comes from singleton helper, not hard-coded literals (grep for singleton helper import in `vision` page)
- No direct collection queries; `cmd /c "npm.cmd run check"` + `build` pass
- Accessibility: each page has one `h1`, correct `h2`/`h3` hierarchy, landmarks, `alt` on any images
- Navigation: header IA still compact (Explore/Journal/About/Participate) — no new top-level nav items added to accommodate these pages (reviewer checks `SiteHeader`)

## Refinement rule
- If `pages/vision` singleton shape from `keystatic.config.ts` does not contain enough structure for the design, extend the singleton via `specs/content-model` discussion and update `CONTENT-MODEL.md` rather than hard-coding vision sections in the page.
