# PBI-030: Entity Rating — Schema Fields + RatingDots Component

## Directive
Add 3 rating integer fields (ratingKnowledge, ratingCreativity, ratingBusiness, 1–3, default 1) to the Keystatic schemas for labs, research, experiments, projects, and opensource collections. Create a `RatingDots.astro` component that renders 3 criteria as filled/empty dots (●●○) in a horizontal row using mono typography.

## Context
- Spec: `specs/entity-rating/spec.md`
- Depends on: none
- Files/modules touched:
  - `keystatic.config.ts` (add 3 fields to 5 collections)
  - `src/components/ui/RatingDots.astro` (new file)
- Plane: — (not synced)

## Verification
- Spec §Contracts: `RatingDots.astro` renders 3 criteria with correct dot count for scores 1–3
- Keystatic schemas for labs, research, experiments, projects, opensource each have `ratingKnowledge`, `ratingCreativity`, `ratingBusiness` integer fields
- All 5 collections validate with min 1, max 3, default 1
- `cmd /c "npm.cmd run check"` passes with 0 errors
- `cmd /c "npm.cmd run build"` succeeds
- Existing content files without rating fields still load (backward-compatible defaults)

## Refinement rule
- If Keystatic integer field validation differs from spec, update the spec to match Keystatic's actual API
