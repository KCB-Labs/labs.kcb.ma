# PBI-013: Open Source Index + Detail + Shared Card System Finalization

## Directive
Complete the discovery surfaces and lock the shared card primitive contract. Create `src/pages/open-source/index.astro` + `src/pages/open-source/[slug].astro` (prerendered, brand 404 on unknown) rendering Open Source entries (name/description/repository/license/status/originLab/relatedProject/technologies/docs) via `OpenSourceCard`, plus finalize `src/components/cards/*` for all entities (`LabCard`, `ProjectCard`, `ArticleCard`, `ResearchCard`, `ExperimentCard`, `PersonCard`, `OrganizationCard`, `OpenSourceCard`) and `src/components/ui/*` (`StatusBadge`, `TypeBadge`, `StageBadge`, `DateLabel`, `ReadingTime`, `RelationLabel`) to a single prop contract used everywhere (homepage, indexes, detail related sections). Ensure no card duplicates markup — each card is the single source for that entity's discovery appearance. Verify via build that every index/detail import uses the shared cards.

## Context
- Spec: `specs/core-pages/spec.md`
- Depends on: PBI-008, PBI-009, PBI-010, PBI-011, PBI-012
- Files/modules touched: `src/pages/open-source/index.astro`, `src/pages/open-source/[slug].astro`, `src/components/cards/*` (final contracts), `src/components/ui/*` (final), `src/components/content/Metric.astro` (optional for OS stats)
- Plane: `kcb/KCBLABS-13`

## Verification
- Spec §Contracts: `npm run build` prerenders `/open-source` and `/open-source/[slug]` for each published OS entry; `/open-source/unknown` → branded 404; detail shows repo link, license, status, originLab, related project, technologies
- Card reuse proof: `LabCard` imported in at least homepage, labs index, and lab detail related block (reviewer checks import graph or `grep -R "LabCard" src`); similarly `ProjectCard`/`ArticleCard` reused across at least two surfaces; `StatusBadge`/`TypeBadge`/`StageBadge` render status colors consistently (typography mono/technical, spec §31)
- No page recreates card markup — duplication check: reviewer greps for card-like div structures outside `src/components/cards`
- Homepage, labs, projects, journal pages still use helpers only; `cmd /c "npm.cmd run check"` + `build` pass
- Images via Astro pipeline with `alt`; badges/status use tokens, not literal colors

## Refinement rule
- If a card needs a variant (e.g., compact vs featured), add a `variant` prop to the existing card rather than forking a second card component, and document variants in `DESIGN-SYSTEM.md`.
