# PBI-026: KCB Logo & Favicon — Branding Integration

## Directive
Add KCB logo image to SiteHeader (replacing text-only brand) and add favicon to BaseLayout. Copy `docs/branding/logo.jpg` to `public/logo.jpg` and `docs/branding/icon.png` to `public/icon.png`. Update SiteHeader to render `<img src="/logo.jpg" alt="KCB Labs" class="logo" />` alongside or replacing the text brand. Add `<link rel="icon" type="image/png" href="/icon.png" />` to BaseLayout `<head>`. Style the logo: height ~32px, vertical-align middle, mixed-blend-mode multiply (for white background removal on warm bg).

## Context
- Spec: `specs/avionics-design-system/spec.md`
- Depends on: PBI-024 (tokens must exist for header styling)
- Files/modules touched: `src/components/astro/SiteHeader.astro`, `src/layouts/BaseLayout.astro`, `public/logo.jpg`, `public/icon.png`
- Plane: `kcb/KCBLABS-26`

## Verification
- `public/logo.jpg` and `public/icon.png` exist
- SiteHeader renders KCB logo image (not just text)
- BaseLayout has favicon link tag
- Logo styled with appropriate height and blend mode
- `cmd /c "npm.cmd run check"` passes
- `cmd /c "npm.cmd run build"` succeeds
- Favicon visible in browser tab

## Refinement rule
- If reality diverges from the spec: stop and flag for human review
