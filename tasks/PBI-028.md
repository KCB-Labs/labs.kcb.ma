# PBI-028: Avionics Dark Theme & Final Verification

## Directive
Ensure dark theme works with Avionics tokens. Update `[data-theme="dark"]` and `prefers-color-scheme: dark` blocks in tokens.css to use Avionics dark palette (dark warm tones, not cool slate). Verify all components render correctly in dark mode. Run full gate suite: check, build, test. Update DESIGN-SYSTEM.md to reference Avionics design system. Create ADR-005 for design system adoption.

## Context
- Spec: `specs/avionics-design-system/spec.md`
- Depends on: PBI-024, PBI-025, PBI-026, PBI-027 (all prior PBIs)
- Files/modules touched: `src/styles/tokens.css` (dark block), `DESIGN-SYSTEM.md`, `docs/adrs/ADR-005.md`
- Plane: `kcb/KCBLABS-28`

## Verification
- Dark theme tokens use warm dark palette (not cool slate)
- `cmd /c "npm.cmd run check"` passes with 0 errors
- `cmd /c "npm.cmd run build"` succeeds
- `cmd /c "npm.cmd test"` passes (24 tests + smoke)
- DESIGN-SYSTEM.md references Avionics design system
- ADR-005 exists with design system decision

## Refinement rule
- If reality diverges from the spec: stop and flag for human review
