# PBI-033: Entity Rating — Detail Page Header Integration

## Directive
Add `RatingDots` to the detail page headers for labs, research, experiments, projects, and opensource. Rating appears in the same row as the `<h1>` title inside the SectionLayout/PageLayout header area. Update the parent pages that render cards to pass rating props.

## Context
- Spec: `specs/entity-rating/spec.md`
- Depends on: PBI-030 (RatingDots component), PBI-031 (data layer must return ratings)
- Files/modules touched:
  - `src/pages/explore/labs/[slug].astro`
  - `src/pages/explore/labs/index.astro` (card grid — pass rating props)
  - `src/pages/explore/projects/[slug].astro`
  - `src/pages/explore/projects/index.astro` (card grid — pass rating props)
  - `src/pages/explore/open-source/[slug].astro`
  - `src/pages/explore/open-source/index.astro` (card grid — pass rating props)
- Plane: — (not synced)

## Verification
- Detail page headers show `RatingDots` next to the `<h1>` title in a flex row
- Index page card grids pass `ratingKnowledge`, `ratingCreativity`, `ratingBusiness` to card components
- `cmd /c "npm.cmd run check"` passes with 0 errors
- `cmd /c "npm.cmd run build"` succeeds

## Refinement rule
- If the header flex layout causes the title to truncate on long titles, add `flex-wrap: wrap` or `min-width: 0`
