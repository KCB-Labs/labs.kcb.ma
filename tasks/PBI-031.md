# PBI-031: Entity Rating — Data Access Layer Integration

## Directive
Update `src/lib/content/labs.js`, `research.js`, `experiments.js`, `projects.js`, and `opensource.js` to include `ratingKnowledge`, `ratingCreativity`, `ratingBusiness` in their returned entity objects. Ensure backward compatibility: entities without rating fields in their YAML default to 1 for all three.

## Context
- Spec: `specs/entity-rating/spec.md`
- Depends on: PBI-030 (schema fields must exist first)
- Files/modules touched:
  - `src/lib/content/labs.js`
  - `src/lib/content/research.js`
  - `src/lib/content/experiments.js`
  - `src/lib/content/projects.js`
  - `src/lib/content/opensource.js`
- Plane: — (not synced)

## Verification
- Every `get*()` and `get*BySlug()` function returns objects with `ratingKnowledge`, `ratingCreativity`, `ratingBusiness`
- Existing seed content (e.g., `src/content/labs/ai-employees.mdoc`) loads without errors — defaults to 1/1/1
- `cmd /c "npm.cmd run check"` passes with 0 errors
- `cmd /c "npm.cmd run build"` succeeds

## Refinement rule
- If the data access layer uses a shared helper for reading content, update the helper once rather than each file individually
