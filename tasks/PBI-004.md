# PBI-004: Docker + Health Endpoint + MCP Config Hardening

## Directive
Close the foundation deployability gate. Create `src/pages/api/health.ts` (`GET /api/health` → `{"status":"ok"}` with `200` JSON, zero third-party deps, `export const prerender = false`), multi-stage `Dockerfile` (node:20 → install → `astro build` → slim runtime copy of `dist/` + prod `node_modules`, `EXPOSE 4321`, `CMD ["node","./dist/server/entry.mjs"]`), `.dockerignore` additions, and verify `.opencode/opencode.jsonc` declares both `Astro docs` MCP (`https://mcp.docs.astro.build/mcp`) and `plane-kcb` MCP. Add lightweight `src/lib/env.ts` helper that reads `KEYSTATIC_*`/`PUBLIC_*` from `process.env`/`import.meta.env` without committing secrets, and document env contract in `DEPLOYMENT.md` stub. No content collections or page content beyond health proof.

## Context
- Spec: `specs/foundation/spec.md`
- Depends on: PBI-001
- Files/modules touched: `src/pages/api/health.ts`, `Dockerfile`, `.dockerignore`, `.opencode/opencode.jsonc` (verify), `DEPLOYMENT.md` (stub with Coolify secrets list), `src/lib/env.ts` (optional helper)
- Plane: `kcb/KCBLABS-4`

## Verification
- Spec §Contracts: `curl localhost:4321/api/health` after `node ./dist/server/entry.mjs` returns `{"status":"ok"}`; Docker build succeeds (`docker build -t labs .`) and `docker run -p 4321:4321 labs` serves the same endpoint (or `npm run build` artifact check confirms `dist/server/entry.mjs` exists and is executable)
- `.opencode/opencode.jsonc` contains both MCP entries with correct URLs/headers (reviewer runs `cat .opencode/opencode.jsonc | grep -q "mcp.docs.astro" && cat .opencode/opencode.jsonc | grep -q "plane-kcb"`)
- `.dockerignore` excludes `node_modules` from build context where appropriate but final image includes prod `node_modules`; no `.env` in image
- `cmd /c "npm.cmd run check"` + `cmd /c "npm.cmd run build"` still green; `/api/health` is not prerendered (`prerender = false` guard)
- No secrets committed: `grep -R "KEYSTATIC_GITHUB_CLIENT_SECRET" --include="*.ts" --include="*.md" src` finds only reference to env var name, not value

## Refinement rule
- If the Node adapter's entry point is not `./dist/server/entry.mjs` at the installed version, correct `Dockerfile` CMD and spec §27/§54 to match the actual adapter output, then flag for human review (do not guess).
