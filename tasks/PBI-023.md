# PBI-023: CI/CD, Documentation & Coolify Deployment Verification

## Directive
Make the delivery loop reproducible and documented. Create/update `.github/workflows/ci.yml` to run `install` → `type check` (`astro check` + `tsc --noEmit`) → `lint` → `test` → `astro build` on every PR (fail on any gate, as per spec §67). Complete `DEPLOYMENT.md` (Docker stages, Coolify Build Pack Dockerfile, Domain `https://labs.kcb.ma`, Port `4321`, Auto Deploy, HTTPS, env var table `KEYSTATIC_GITHUB_CLIENT_ID/SECRET`, `KEYSTATIC_SECRET`, `PUBLIC_KEYSTATIC_GITHUB_APP_SLUG`, `PUBLIC_SITE_URL`, health endpoint, GitHub→Coolify content workflow `Editor→/keystatic→GitHub commit→webhook→Docker→labs.kcb.ma`), `CONTRIBUTING.md` (branch strategy `main` + `feature/*`, commit conventions, PBI workflow, review gates, AI workflow Understand→Inspect→MCP→Design→Implement→Test→Build→Review→Commit), finalize `CONTENT-MODEL.md` + `DESIGN-SYSTEM.md`, and ensure `docs/adrs/` contains ADRs for `output:server`, storage, relationship-first model, and React island policy (or stubs referencing specs if formal ADR text is deferred). Verify Coolify deploy: `docker build` locally succeeds, `/api/health` is reachable, and `DEPLOYMENT.md` steps map to actual Coolify application creation. Keep gate commands deterministic via `cmd /c "npm.cmd ..."` as declared in `AGENTS.md` §4.

## Context
- Spec: `specs/production-hardening/spec.md`
- Depends on: PBI-001, PBI-004, PBI-008, PBI-020, PBI-021, PBI-022
- Files/modules touched: `.github/workflows/ci.yml`, `DEPLOYMENT.md`, `CONTRIBUTING.md`, `CONTENT-MODEL.md`, `DESIGN-SYSTEM.md`, `ARCHITECTURE.md` (final as-built update), `docs/adrs/ADR-*.md`, `package.json` (CI scripts if needed)
- Plane: `kcb/KCBLABS-23`

## Verification
- Spec §Contracts: CI workflow file exists and sequentially invokes gates declared in `AGENTS.md` §4 (`install` → `check` → `lint` → `test` → `build`); a PR with a failing `check` blocks merge (reviewer inspects YAML; local `cmd /c "npm.cmd run check"` + `build` still pass on `master`)
- `Dockerfile` multi-stage builds locally (`docker build -t labs .` succeeds) and `docker run -p 4321:4321 labs` serves `/api/health` → `{"status":"ok"}`; image contains only `dist/` + prod `node_modules` (no `.env`)
- `DEPLOYMENT.md` documents Coolify Build Pack Dockerfile, domain/port/env/health correctly and matches the actual `astro.config.ts` site/adapter; `CONTRIBUTING.md` describes branch strategy + PBI workflow; `CONTENT-MODEL.md` fully documents 9 collections + singletons; `ARCHITECTURE.md` updated to reflect as-built after all PBIs (not the onboarding placeholder)
- `cmd /c "npm.cmd run check"` + `npm.cmd test` (if Vitest adopted) + `npm.cmd run build` all green; health endpoint has no third-party deps
- No secrets committed: `grep -R "\.env" --include="*.md"` finds only `.env.example` reference style, not values

## Refinement rule
- If Coolify's Docker build requires a different context or health check annotation than documented, update `DEPLOYMENT.md` and `Dockerfile` together and flag for human review before marking Done (deployment topology is a constitution-level decision).
