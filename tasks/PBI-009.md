# PBI-009: Homepage — Hero + Current Work + Featured Sections + Numbers

## Directive
Build the homepage as the 30-second explainer (spec §79). Compose `src/pages/index.astro` (prerendered) using `BaseLayout`, `Container`, `Section`, `Prose`, tokens, and helpers from `src/lib/content` only. Sections in order: Hero (headline "We explore ideas that could become technology, products and companies.", supporting sentence, CTAs `Explore Labs` → `/labs`, `Explore Projects` → `/projects`, `Support the Labs` → `/participate/sponsorship`), Current Labs (curated LabCards from `getActiveLabs()` featured first), Current Work strip (EXPLORING/BUILDING/SHARING driven by real Lab/Project/OS research), selected Projects (ProjectCards with Type/Stage/Origin Lab), Research/Experiments highlight (ResearchCard/ExperimentCard), latest Journal (ArticleCards with category/title/excerpt/date/readingTime/related), Numbers (Labs/Experiments/Validated Projects/Open Source counts computed via helpers, never hard-coded), Ecosystem ("Built with an ecosystem" with OrganizationCards, not a generic logo wall), final CTA "Research is stronger when more people participate." → Collaborate/Support. Reuse cards created here or stubbed for next PBIs; this PBI owns the homepage assembly and any card stubs it introduces, but cards become shared primitives.

## Context
- Spec: `specs/core-pages/spec.md`
- Depends on: PBI-003, PBI-008
- Files/modules touched: `src/pages/index.astro`, `src/components/sections/*` (Hero, CurrentLabs, CurrentWork, Numbers, Ecosystem, ParticipateCTA), `src/components/cards/LabCard.astro`, `src/components/cards/ProjectCard.astro`, `src/components/cards/ArticleCard.astro` (stubs/full as needed), `src/components/ui/StatusBadge.astro`, `src/components/ui/TypeBadge.astro`, `src/components/ui/StageBadge.astro` (minimal)
- Plane: `kcb/KCBLABS-9`

## Verification
- Spec §Contracts: `npm run build` prerenders `/` to `dist/index.html`; view-source contains all 8 homepage sections with CTAs pointing at correct `href`s; Numbers values equal `getLabs().length` etc. — unit test asserts homepage data loader calls helpers (or integration test fetches `/` and asserts data-derived numbers vs helper counts)
- No hard-coded counts/cards: removing all labs fixtures except one reduces Numbers + Current Labs accordingly on next build (reviewer can verify by inspecting data loader, not literals in `index.astro`)
- All homepage data comes via `src/lib/content` — grep gate `grep -R "getLabs\|getProjects" src/pages/index.astro` shows helper imports, not `getCollection`
- Accessibility: homepage has one `h1` (hero headline), correct `h2` per section, landmarks, all images `alt`; `astro check` passes
- Cards reuse expectation: `LabCard`/`ProjectCard`/`ArticleCard` accept typed props and render Status/Type/Stage badges consistently

## Refinement rule
- If homepage content model needs a `featured`/`priority` sort not yet in the schema, use the existing `featured`/`priority` fields from `specs/content-model` and document sort order in the PR; do not add new collection fields without spec update.
