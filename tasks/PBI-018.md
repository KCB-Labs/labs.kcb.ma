# PBI-018: Explore Filtering — Labs Status + Projects Type/Stage

## Directive
Progressively enhance the already server-rendered Explore pages with filter islands. Build `src/components/react/Filters.tsx` (supporting `LabsFilters` and `ProjectsFilters` variants) with values: Labs `Active/Exploring/Paused/Archived` (spec §24) and Projects `All/Internal/Client/Startup/Collaboration/Open Source/Research` × stage `Idea/Validating/Validated/Implementation/Live/Completed/Spun Out` (spec §24). Wire labs to `src/pages/labs/index.astro` and projects to `src/pages/projects/index.astro` so: server-rendered full list is baseline (no JS → all published cards visible), island controls filter the visible cards client-side and sync URL query params (`?type=Internal&stage=Live`), and refreshing with params restores the filtered state. Use `client:visible` hydration and derive option lists from schema enums, not hard-coded literals duplicated across files.

## Context
- Spec: `specs/interactive-features/spec.md`
- Depends on: PBI-010, PBI-011, PBI-017
- Files/modules touched: `src/components/react/Filters.tsx` (or `FiltersL.tsx`/`FiltersP.tsx` split), `src/pages/labs/index.astro` (integration), `src/pages/projects/index.astro` (integration), `src/lib/content/labs.ts`/`projects.ts` (enum exports if not already)
- Plane: `kcb/KCBLABS-18`

## Verification
- Spec §Contracts: `/projects` server-rendered shows all projects without JS (E2E with JS disabled or manual verification note + unit test for filter predicate); with JS, selecting type `Internal` shows only `type===Internal` cards and URL becomes `?type=Internal`; refresh restores filter (Playwright or unit test for param sync + predicate)
- Same for labs: `Active` filter narrows to `status===Active` and param `?status=Active` syncs; stage filter for projects includes all 8 stages exactly — unit test asserts predicate matches spec §24 values verbatim
- Filter options derived from schema enums (reviewer checks `Filters.tsx` imports enum from `src/lib/content` or `keystatic.config` re-export, not a separate literal list)
- Hydration gate: `Filters.tsx` uses `client:visible` (or `client:idle` with justification) — `grep -R "client:" src/pages/labs src/pages/projects` shows only `Filters` islands as React
- `cmd /c "npm.cmd run check"` + `build` pass; no layout/header turned React

## Refinement rule
- If URL param sync requires Astro middleware or Astro `useState` pattern different from assumed, verify via Astro MCP and update only the integration wiring; keep filter predicates as the source of truth and flag for review.
